node_modules/
dist/
dist-cpanel/
# Clave real de la API de Claude — nunca al repo público. Solo vive en el
# servidor (cPanel), copiada a mano desde config.example.php. Ver [[project_api_claude_tutelas]].
robot-tutelas/config.php
